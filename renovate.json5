{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  extends: [
    "config:recommended"
  ],
  rebaseWhen: "behind-base-branch",

  pinDigests: true,

  packageRules: [
    {
      matchPackageNames: ["*"],
      minimumReleaseAge: "7 days",
    },
    {
      matchPackageNames: ["netbox-community/netbox"],
      minimumReleaseAge: "5 days",
    }
  ],
  customManagers: [
    {
      customType: 'regex',
      managerFilePatterns: [
        'netbox_version.env'
      ],
      matchStrings: [
        '# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?\\s\\S+=(?<currentValue>\\S+)',
      ],
      versioningTemplate: '{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}',
      autoReplaceStringTemplate: "// renovate: datasource=docker{{{indentation}}}from '{{{depName}}}:{{{newValue}}}@{{{newDigest}}}'"
    },
  ],
}
